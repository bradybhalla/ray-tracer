(executable
 (name single_render)
 (libraries ray_tracer))

(executable
 (name animation)
 (libraries ray_tracer))

(env
 (dev
  (flags
   (:standard -warn-error -A))))

; (env
;  (dev
;   (flags
;    (:standard -warn-error -A))
;   (ocamlopt_flags
;    (:standard -unbox-closures
;           -inline 1000
;           -rounds 10
;           -nodynlink
;           -O3))))
